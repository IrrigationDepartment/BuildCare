name: BuildCare
description: "A new Flutter project."
# Prevent accidental publishing to pub.dev
publish_to: 'none' 
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^4.2.0 # Use the latest version
  firebase_auth: ^6.1.1 # Use the latest version
  cloud_firestore: ^6.0.3 # Use the latest version
  crypto: ^3.0.3 # Add this line
  intl: ^0.20.2           # Use your version
  rxdart: ^0.28.0
  firebase_storage: ^13.0.4
  image_picker: ^1.2.1
  http: ^1.1.0
  path: ^1.8.0
  syncfusion_flutter_charts: ^32.1.23 # Use the latest version
  url_launcher: ^6.2.3
  cached_network_image: ^3.3.0
  pdf: ^3.10.7
  printing: ^5.11.1
  path_provider: ^2.1.1
  share_plus: ^7.2.0
  dio: ^5.4.0  # For downloading images
  firebase_core: ^3.1.1 # Use the latest version
  firebase_auth: ^5.1.1 # Use the latest version
  cloud_firestore: ^5.0.2 # Use the latest version
  firebase_storage: ^12.4.10 # Compatible with firebase_core ^3.1.1
  intl: ^0.19.0 # Added for date formatting
  image_picker: ^1.0.4 #  Add this line for image functionality

  http: ^1.2.1
  http_parser: ^4.0.2
  file_picker: ^8.0.0 # Check pub.dev for the latest version and use a flexible constraint.
  

  #----------------------------------------------------
  # Firebase (Your existing packages)
  #----------------------------------------------------
  firebase_core: ^3.1.1 
  firebase_auth: ^5.1.1
  cloud_firestore: ^5.0.2

  #----------------------------------------------------
  # Common Firebase Add-ons
  #----------------------------------------------------
  # For uploading files (like images, PDFs) to Firebase
  firebase_storage: ^12.1.0 
  # For handling push notifications
  firebase_messaging: ^15.0.2 

  #----------------------------------------------------
  # State Management
  #----------------------------------------------------
  # A simple and common way to manage your app's state
  provider: ^6.1.2 

  #----------------------------------------------------
  # UI & UX Packages
  #----------------------------------------------------
  # For easily using any of the 1,000+ fonts from fonts.google.com
  google_fonts: ^6.2.1 
  # To show images from the internet with caching and placeholders
  cached_network_image: ^3.3.1 
  # For picking images or videos from the phone's gallery or camera
  image_picker: ^1.1.2 
  # A collection of beautiful loading indicators
  flutter_spinkit: ^5.2.1 

  #----------------------------------------------------
  # Utility Packages
  #----------------------------------------------------
  # For making HTTP requests to other APIs
  http: ^1.2.1 
  # For formatting dates, times, numbers, and currencies
  intl: ^0.19.0 
  # For saving simple key-value data (like user settings)
  shared_preferences: ^2.2.3 
  # For opening web URLs, sending emails, or making phone calls
  url_launcher: ^6.3.0 
  # For finding the correct local file paths on the device
  path_provider: ^2.1.3 
  cloud_functions: ^5.6.2

  
  # Firebase Core & Authentication
  firebase_core: ^4.2.0
  firebase_auth: ^6.1.1
  
  # Firebase Database & Storage
  cloud_firestore: ^6.0.3
  firebase_storage: ^13.0.4
  
  # Firebase Additional Services
  firebase_messaging: ^16.1.0
  cloud_functions: ^6.0.5
  
  # State Management
  provider: ^6.1.2
  rxdart: ^0.28.0
  
  # UI & UX Packages
  google_fonts: ^6.2.1
  cached_network_image: ^3.3.1
  flutter_spinkit: ^5.2.1
  syncfusion_flutter_charts: ^32.1.23
  
  # Image & File Handling
  image_picker: ^1.2.1
  file_picker: ^10.3.8
  
  # Utility Packages
  intl: ^0.20.2
  http: ^1.2.1
  http_parser: ^4.0.2
  shared_preferences: ^2.2.3
  url_launcher: ^6.3.0
  path_provider: ^2.1.3
  path: ^1.9.0
  crypto: ^3.0.3
  foundation: ^0.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  flutter_lints: ^5.0.0

  #----------------------------------------------------
  # App Branding (Run these from the terminal)
  #----------------------------------------------------
  # For automatically creating your app launcher icons
  # Run: dart run flutter_launcher_icons
  flutter_launcher_icons: ^0.13.1 

  # For setting up your app's native splash screen
  # Run: dart run flutter_native_splash:create
  flutter_native_splash: ^2.4.0 


# The following section is specific to Flutter.
flutter:
  uses-material-design: true

  # To add assets to your application, add an assets section.
  # I've changed your 'lib/src/' path to a more standard setup.
  assets:
    - assets/images/
    - assets/icons/
    # Example:
    # - assets/images/logo.png
    # - assets/icons/home_icon.svg

  # To add custom fonts, uncomment this section.
  # (Note: google_fonts package above is much easier)
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - lib/src/
